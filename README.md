# AKS Scripts

Follow this steps:
1. Run ./gen-config.sh to generate YAML files for control plane and worker nodes.
2. Run ./gen-patch.sh to generate patch files for mounting the extra disk into the worker nodes
